# API実装タスクの設定ファイル例
# 使用方法: e8 run --config examples/api-implementation.yaml

task: "ユーザー管理APIエンドポイントを実装"
criteria:
  - "GET /api/users が200を返し、ユーザー一覧を取得できる"
  - "POST /api/users でユーザーを作成できる"
  - "GET /api/users/{id} で特定ユーザーを取得できる"
  - "すべてのAPIテストがパスする"

max_iterations: 10
